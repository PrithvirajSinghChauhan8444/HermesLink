# Core Directory (`src/core`)

This directory contains the core logic for managing the application state.

## Files

- `job_manager.py`: Responsible for creating, updating, and persisting download jobs (currently empty/placeholder).
