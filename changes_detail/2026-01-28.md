# Changes Detail - 2026-01-28

**Working Branch:** `frontend`

## Completed Tasks

- **Added Animated Components:**
  - Implemented `DecryptedText` for the landing page title.
  - Added `GlassSurface` effect behind the title.
- **Implemented Dock Navigation:**
  - Created `Dock` component using `framer-motion` and `react-icons`.
  - Replaced standard Navbar with Dock.
  - Implemented positioning logic: Center on Landing Page (post-interaction), Bottom on all other pages.
- **Project Restructuring:**
  - Reorganized `src/pages` directory.
  - Created individual subfolders for each page (e.g., `src/pages/About/`).
  - Co-located CSS files with their respective JSX components.
  - Updated import paths in `App.jsx` and page components.

## Current Issues

- On moving from one page to another, the dock "goes up and appears below" (transition artifact between center/bottom states).
