# 2026-01-24

## Work Summary

- **Working Branch**: `queue`
- **Main Feature**: Implemented Multi-Queue Download Scheduler.
- **Status**: No error so far, testing left.

### Key Changes

1.  **Job Manager (`src/core/job_manager.py`)**:
    - **Multi-Queue Architecture**: Moved from single `self.queue` list to `self.queues` dictionary with `QueueState`.
    - **Scheduler Upgrade**: Implemented `process_queues()` to handle priority-based scheduling and enforce global single-job limits.
    - **Persistence**: Updated `jobs.json` schema to save/load queue configurations and states automatically.
    - **Queue Management API**: Added `create_queue`, `delete_queue`, `enqueue`, `dequeue`.

2.  **Models (`src/core/models.py`)**:
    - Added `QueueConfig` (priority, parallel limits) and `QueueState` models.
    - Updated `Job` model with `queue_id`.

3.  **CLI Controller (`src/controller.py`)**:
    - **Manage Queues**: Added menu to view, create, and delete queues.
    - **Smart Add**: "Add New Download" now prompts for Queue selection/creation.

4.  **Dashboard (`src/dashboard.py`)**:
    - **Interactive Mode**: Added menu (`M` key) and queue filtering.
    - **Visualization**: Added "Queue" column to job list.

### Verification

- Ran `test_multi_queue.py`: Passed all 6 tests (Priority, Invariant, Auto-Advance, Persistence).
