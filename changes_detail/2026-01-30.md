# Changes Detail - 2026-01-30

## Frontend UI Overhaul

### Layout Restructuring
- **REMOVED** `MainLayout` wrapper from `App.jsx`. Routes are now top-level.
- **RESTORED** Title and Navbar explicitly within `LandingPage.jsx` to maintain the "Hub" experience without the global layout.
- **ADDED** Global `<Navbar />` to `App.jsx` so it appears at the bottom of every page in the application.

### Active Jobs Page
- **INTEGRATED** `Threads` animated component as a fixed full-screen background (activeJobs page).
- **UPDATED** Layout to be independent of the previous global styles, ensuring proper layering (z-index) between the background animation and the content.

### Page Standardization
- **APPLIED** Standardized "Glass Card" layout to all secondary pages (`History`, `QueueManagement`, `Settings`, `About`).
- **CREATED** Specific container classes for each page:
  - `.history-page`
  - `.queue-page`
  - `.settings-page`
  - `.about-page`
- **IMPLEMENTED** Consistent CSS for these pages:
  - Black background.
  - Centered content with `dashboard-padding`.
  - Glassmorphic card style for `dashboard-placeholder-card`.
