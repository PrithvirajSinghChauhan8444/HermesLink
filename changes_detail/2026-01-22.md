# Changes Log - 2026-01-22

## Work Overview

- **Branch**: `Queue`
- **Focus**: Implementing bulk download support and kill switch functionality.

## Completed Tasks

1.  **Bulk Downloads**:
    - Implemented `src/utils/url_utils.py` to strip and extract multiple URLs from a single input string.
    - Updated `src/controller.py` to queue these URLs individually.
2.  **Kill Switch**:
    - Added `stop_all_jobs` method to `src/core/job_manager.py` to stop all PENDING, RUNNING, and PAUSED jobs.
    - Added `kill_switch` function to `src/controller.py` with 5-digit verification code ("CRITICAL: KILL SWITCH INITIATED").

## Known Issues (To Fix)

1.  **Multiple Downloads with Same Link**:
    - Single downloads work fine.
    - When multiple identical links are provided (e.g. `url1httpurl1`), the system generates the same Job ID for them.
    - This causes conflicts where the second job overwrites or conflicts with the first one.

## Next Steps

- Implement unique Job ID generation (likely by appending a uuid or timestamp) to allow duplicate URLs to exist as separate jobs.
