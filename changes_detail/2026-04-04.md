# Documentation of Changes – 2026-04-04

## 1. Dynamic Per-Profile Subfolder Persistence
* **Files Modified:** `backend/config.yaml`, `backend/src/test_agent.py`
* **Details:** Updated the configuration structure to support per-profile dynamic nesting of downloaded absolute folder paths (e.g., `storage_profiles[prof_id]['subfolders']`). Modified `test_agent.py` to auto-capture the complete destination `output_path` derived strictly out of resolving user input cleanly into its specific storage profile safely.
* **Outcome:** Provides complete memory parity to the backend so the agent retains and dynamically synchronizes all unique download output directories created recursively by the user.

## 2. Interactive Frontend Subfolder Autocomplete
* **Files Modified:** `frontend/HermesLink_frontend/src/hooks/useDevices.js`, `frontend/HermesLink_frontend/src/components/features/NewJobModal.jsx`
* **Details:** Directly integrated the nested `subfolders` config mapping seamlessly into the frontend API via Firebase RTDB presence payloads without using global arrays. Constructed a clean native HTML `<datalist>` onto the optional download modal 'Sub-directory' field tightly coupled exclusively to the currently selected `selectedDevice` storage profile properties.
* **Frontend UI Implementation:** The frontend now natively presents all past created directories as autocomplete options. To maximize aesthetic UX readability, extended custom extraction logic ensures the datalist shows strictly just the relative directory label (via `pop()`) resulting in visually-clean folder text selections. Appended explicit `showPicker()` logic bounded to `onFocus` and `onClick` DOM events forcing instant single-click dropdown list recommendations over standard rigid browser defaults. Added helpful placeholder text natively onto the search constraint field.
