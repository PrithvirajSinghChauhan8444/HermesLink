# Changes Detail - 2026-02-01

## Architecture & Navigation
- **REVERTED** to Single Layout Architecture (`MainLayout` controls everything) to fix animation consistency.
- **RESTORED** State-based navigation (`activeTab` state) to replace `react-router-dom` for internal page switching.
- **CENTRALIZED** Navbar logic in `MainLayout`, removing duplicate instances from child pages (`ActiveJobs`, `Queue`, etc.).

## Visual Layout & Design
- **FIXED** Background constraint issues; pages now span the full viewport width.
- **REMOVED** `max-width` and panel styling from the main `layout-content` wrapper.
- **CENTERED** Page content using a refactored `.dashboard-padding` class (`max-width: 75rem`, `margin: 0 auto`).
- **REFACTORED** All styling to use `rem` units instead of pixels (`px`) for better scalability.

## Header Logic
- **UPDATED** Header visibility logic: The "HermesLink" title is now **conditional**.
- **VISIBLE** Only on the Landing Page (Home).
- **HIDDEN** On all other application pages to provide a clean, full-screen interface.
